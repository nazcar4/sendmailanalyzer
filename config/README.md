# Configuration templates

Public configuration templates for SendmailAnalyzer, Apache integration and runtime setup. Site-specific settings belong in local files such as `/etc/sendmailanalyzer.d/*.conf` and are not stored in the public source tree.
