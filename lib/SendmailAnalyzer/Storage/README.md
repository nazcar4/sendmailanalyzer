# Persistent storage

`SQLite.pm` implements the SendmailAnalyzer v10 SQLite persistence layer, including schema management, normalized events, message correlation, indexes, statistics and query helpers. Runtime databases are never stored in this repository.
