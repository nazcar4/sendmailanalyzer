# Build and installation helpers

`build-deb.sh` builds the Debian package and `install.sh` provides source-tree installation support. Installing the published `.deb` with APT is preferred for normal Debian deployments.
