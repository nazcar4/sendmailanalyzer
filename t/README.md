# Test suite

Perl tests covering parsers, storage, classification, migration, web behavior, packaging, security and public-release hygiene. Run the complete suite from the repository root with `prove -lr t`.
