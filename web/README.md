# Web application

`sendmailanalyzer.psgi` implements the read-only reporting interface, including HTML reports, calendar navigation, message details, search, JSON API and Prometheus-style metrics. It binds locally by default for reverse-proxy deployment.
