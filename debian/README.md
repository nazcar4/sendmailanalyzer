# Debian packaging

Debian package metadata, dependencies, build rules, changelog and package lifecycle scripts. This directory controls how the SendmailAnalyzer `.deb` package is built, installed, upgraded and removed.
